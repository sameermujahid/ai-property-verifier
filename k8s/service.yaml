apiVersion: v1
kind: Service
metadata:
  name: ai-property-verifier
  labels:
    app: ai-property-verifier
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: ai-property-verifier 